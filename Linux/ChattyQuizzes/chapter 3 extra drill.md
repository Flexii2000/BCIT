
# 🔥 **Weak-Spot Drill (Short Answer Only)**

### **1. Write the correct command to create a symlink named `configlink` pointing to `/etc/config`.**

_(Order matters!)_

---

### **2. What does the command below do?**

```
grep -rl "nginx" /var/www
```

---

### **3. Write a `grep` command that ignores case and matches whole words for “error” in `/var/log/syslog`.**

---

### **4. What does `grep -c "set" ~/.bashrc` output?**

---

### **5. Write a `find` command that lists all files under `/home` larger than 2 MB.**

---

### **6. Why can you NOT create a hard link to a directory?**

_(One short sentence.)_

---

### **7. Does a symlink point to an inode or a path?**

_(One word answer.)_

---

### **8. In Vim, what does `ci"` do?**

_(Short explanation.)_

---

### **9. In Vim, what does `ca"` do?**

_(Short explanation, contrast with #8.)_

---

### **10. Write the Vim search command to look for the word “failed” inside a file.**

---

Send me your answers and I’ll grade them.